# GPT System Prompt

Status

Draft

---

## Purpose

This document will eventually contain the production system prompt used by the AWS JobPilot Mentor.

The prompt is intentionally excluded during Version 1 because it is still undergoing rapid iteration.

Once the mentor reaches Version 2.0, the prompt will be version-controlled here so it can be reproduced exactly.

---

## Current Source

The active system prompt is maintained inside the Custom GPT configuration.

Future releases will synchronize the GitHub version with the deployed GPT.
